# T63 EV通知閾値再導出

仕様: [SPEC-T63](codex/SPEC-T63-ev-threshold-rederivation.md)  
実装日: 2026-07-26

## 実装

`backtest_t63_ev_threshold.py` に、固定6閾値、2段階fail-closedゲート、
発走前最終の有効スナップショット選択、反実仮想EV、開催日block bootstrapを実装した。
台帳のグリッド・ゲート・prospective開始日と、SPEC／本番モデル／契約の3 SHAを
実行ごとに照合する。本番モデルSHAが変わった場合は集計せず再登録を要求する。

本番の `jra_ev.py`、通知関数、閾値1.3、ロギングDBには変更を加えていない。

## 2026-07-26 ゲート状態

| 項目 | 現在 | 必要 | 状態 |
|---|---:|---:|---|
| 開催日 | 2 | 4 | 未達 |
| レース | 69 | 200 | 未達 |

分布ゲート未達のため、仕様どおり校正比、回収率、EV分布、候補別通知数は
読み取り・出力していない。結果JSONも作成していない。

次回は4開催日かつ200レース到達後に同じハーネスを再実行し、分布形状だけを出力する。
裁定用の全指標は12開催日かつ上位モデルが指定した固定グリッド内の最有力閾値で
反実仮想通知300件以上になるまで封印する。

## 2026-08-29 v2改訂

仕様: [SPEC-T63 v2](codex/SPEC-T63-ev-threshold-rederivation-v2.md)
(台帳 `T63-ev-threshold-rederivation-v2`、`superseded_by`で`T63-ev-threshold-rederivation-v1`を参照。
v1行は不変・append-only)。

### 改訂の理由

v1の裁定ゲート②「12開催日 ∧ 最有力閾値で反実仮想通知300件」は、起票時点の見込み
(旧モデル時代は1開催日に27〜88頭がEV≥1.3) を前提にした件数だった。T45新モデルの
EV分布 (ゲート①通過時に読了: p50=0.765 / p90=0.923 / p99=1.067、日次最大1.03〜1.18)
では、2026-08-29時点の**件数カウントのみ** (ハーネスの`--leading-threshold`指定時の
`counterfactual_notifications`、仕様で許可された読み取り) が以下で、300件は最も緩い
1.00でも2027年1〜2月、それ以外は事実上到達不能:

| 候補閾値 | 反実仮想通知 (11開催日/374R) |
|---|---:|
| 1.00 | 63 |
| 1.05 | 29 |
| 1.10 | 12 |
| 1.15 | 4 |
| 1.20 | 1 |
| 1.30 | 0 |

**閾値別の校正比・回収率・その他の指標は一切読んでいない**。本改訂はゲート件数の
前提崩れ (分布シフト) に対する手続き上の是正であり、結果を見た基準変更ではない。

### v1から変更した点 (これのみ)

- 裁定ゲート②: 「12開催日 ∧ 最有力閾値で反実仮想通知 **100件** 以上」に改める
  (300→100)。開催日要件12は不変
- 最有力閾値 (leading threshold) を **1.00** に指定 (上位モデル指定。件数から
  唯一現実的に到達し得る候補であり、他候補の指標は裁定時に同時出力される)
- ハーネス: `EXPERIMENT_ID` / `ADJUDICATION_NOTIFICATIONS` / `SPEC_SHA256` /
  `CONTRACT_SHA256` / `DEFAULT_SPEC` を v2 に更新

データ源・cutoff定義・母集団params・候補グリッド・一次/二次/安全指標・ゲート①・
本番モデルSHA・prospective開始日 (2026-07-25) は不変。全DB読み取り専用のまま。
本番の通知条件は本SPECの範囲外 (→ SPEC-T71 で別途扱う)。ゲート②到達前に校正比・
回収率を出力しないfail-closed構造も不変。

見込み: 1.00で1開催日≈5.7件 → 100件到達はあと約7開催日 (2026年10月上旬〜中旬)。
到達後にハーネスを`--leading-threshold 1.00`で実行し、上位モデルが裁定する。
