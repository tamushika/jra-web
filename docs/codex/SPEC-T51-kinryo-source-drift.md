# SPEC-T51: 2026年runsのソースドリフト是正 (kinryo欠損37.4%)

起票: 2026-07-19 (Fable 5)。T41で発見したデータ品質問題の是正。
**T45定期再学習およびあらゆる斤量系特徴の再挑戦の前提条件。**

## 0. 実行順序の制約 (最重要)

- **ability.db は実験 `T50-moisture-cushion-v1` にsha256封印済み (2026-07-19登録)。
  本タスクのDB書き込みはT50評価 (Stage 2) の完了後にのみ実行する** — 先に書き込むと
  T50評価がハッシュ検査で停止する。調査・実装・テスト・dry-runは先行してよい
- netkeiba日次制限: 他のスクレイプタスクと同時に走らせない。1秒スリープ厳守
- 書き込み前に ability.db をバックアップ (`backups/` へ日付付きコピー)

## 1. 背景 (T41の発見)

2026年runs (netkeiba埋め戻し由来) は:
- `kinryo` NULL 37.4% (2025年TARGET由来は17.7%)
- race_nameがnetkeiba正式名称で、TARGET式ハンデ「Ｈ」マーカー0件
→ 斤量特徴が2026年だけ系統的に欠け、ハンデ戦判定も経路依存で壊れている。

## 2. 作業内容

### Stage A: 調査 (読み取りのみ・先行可)

1. 2026年kinryo NULL行の分布を報告 (月別・場別・レース種別。どの取得経路が原因か特定)
2. netkeiba成績/出馬表ページでの斤量の所在と、**ハンデ戦判定の代替ソース**
   (レース条件欄の「ハンデ」表記等) を特定し、判定規約を文書化
3. 必要リクエスト数を見積もる (対象レース数×ページ種)

### Stage B: 実装+dry-run (先行可)

1. 埋め戻しスクリプト (隔離・新規ファイル)。**列名明示INSERT/UPDATE**
   (runsは列が増える前提。extend_ability_from_neon の fukusho_pay 退避と同じ配慮)
2. `--dry-run` で更新予定件数・サンプル20行を出力し、書き込み0で報告
3. ハンデ戦判定は既存マーカーを**上書きせず**、netkeiba由来の判定を別扱いで付与
   (2025年以前のTARGET式判定との混在を作らない)

### Stage C: 書き込み (T50評価完了後・上位モデル承認後)

1. バックアップ → 実行 → 前後の欠損率・行数不変・他列非破壊をレポート
2. 実行後の ability.db sha256 を報告 (以後の実験封印はこの新ハッシュ)
3. extend_ability 定常経路での斤量取得を確認し、再発防止をHANDOFFに1行追記

## 3. テスト

1. dry-runが書き込みゼロであること
2. 更新対象の選定 (NULLのみ・2026年のみ) が正しいこと (合成DB)
3. 非対象列・非対象年の行が変わらないこと (前後比較)
4. `python -m pytest tests/ -q` 全体パス

## 4. 成果物

Stage A報告 → dry-runレポート → (上位モデル承認) → 書き込み後レポート。0.5-1日想定。
