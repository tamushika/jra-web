# T17a 実行前監査と仕様レビュー (2026-09-24, Fable 5.1)

状態: **監査・レビューのみ。性能評価・学習・本番変更・正式台帳への追記は未実施。**
対象: [SPEC-T17a v1](codex/SPEC-T17a-odds-drift-diagnostic.md) (2026-09-24 起票) → 本レビューで v2 に改訂。
データ: `data/jra_logging.db` を `mode=ro` + `PRAGMA query_only=ON` で読取 (2026-07-11 以降の時点別スナップショット)。

## 1. 監査結果 (2026-09-24 時点)

### 1.1 蓄積量

| 項目 | 値 |
|---|---|
| 開催日 (stage 30 取得あり) | 20 日 (7月 4 / 8月 10 / 9月 6)。9/13 は欠測 |
| 30/15/10/5/2 の 5 段階が揃うレース | 616 (30/10/2 だけでも 616 = 段階欠けは無い) |
| 厳密 clean (品質フラグ無し・窓内取得・有効オッズ=頭数・8 頭以上) | **570** (8〜11 頭 132 / 12〜15 頭 280 / 16 頭以上 158) |
| 結果照合 (race_results に 1 着行あり) | 616/616 (official_status はすべて official) |
| 結果行数 ≠ 30 分前の頭数 | 74 レース (取消・除外 58 頭分。30 分前以降の取消を含む) |
| 1 日あたりレース数 | 7〜8 月 35 (3 場)、9 月 23 (2 場)、9/21 (月) 11 |

### 1.2 取得時点の実測

| stage | 発走までの秒数 p10 / p50 / p90 | 規約窓 (stage×60−60 以上) の充足率 | 品質フラグ付き取得 |
|---|---|---|---|
| 30 | 1,774 / 1,784 / 1,793 | 96.6% | 3.0% |
| 10 | 577 / 585 / 594 | 99.2% | 2.5% |
| 2 | 98 / 107 / 115 | 99.9% | 1.8% |

- 最小値は −11〜−25 秒 (発走後の取得) が僅かにある。**発走後取得は必ず除外**する。
- フラグの内訳は late_capture 413 / late_capture+post_time_changed 215 / catchup_burst 119 / scheduler_restart 66 (取得単位)。
- 2 分前の実体は「発走 約 107 秒前」。最終オッズ (確定) との差は残る。

### 1.3 モデル確率の時点

- `predictions` は 1 レースに 6〜14 回の解析があり、典型は発走の 97・78・30・30・15・15・10・10・5・5・2・2 分前 (朝の解析 + 各 stage の再解析)。
- 30 分以上前の予測が存在するレース 606/618。**stage 30 の再解析はその時点のオッズ (ln_odds 特徴) を使う**ので、「モデル − 市場@30」の乖離は同時刻の残差として定義できる。ただし `feature_snapshot_json` に ln_odds は保存されておらず、予測行とオッズ取得の対応 (fetch_id / 時刻差) は実装時に検証が必要。
- `ev_evaluations` は 15 分前・5 分前の通知判定にしか紐づかない (T63 契約の対象)。T17a はこれを読まない。

### 1.4 CL 特徴との関係 (重要)

現行 CL の 21 特徴には `ln_odds` (解析時点の単勝オッズ) が含まれる。したがって「モデル確率」は市場から独立ではなく、解析時刻のオッズを既に織り込む。T17a の候補 (d) はこの点を明示したうえで「同時刻オッズを見たモデルが、その後の市場の動きと結果をどれだけ先読みするか」の診断に限定する。

## 2. 800R 先行診断 (D3) と 2,000R 勝馬精度検証の区別

| 観点 | D3 先行診断 (T17a) | 勝馬精度検証 (T17 本体、未起票) |
|---|---|---|
| 問い | 市場の時間変化 (30→2 分前) は 2 分前の市場確率だけでは説明できない勝馬情報を持つか | 時点別オッズ由来の特徴を CL に加えると、**発走前の固定時点**で勝率・top-k が改善するか |
| モデル | 市場のみ (レース内 conditional logit、係数はレース横断で共通、3 候補) + 参考で CL 残差 | 現行 21 特徴 + ドリフト特徴の CL (学習を伴う) |
| 情報時点 | 特徴 = stage 30/10/2 の取得値 (発走 107 秒前まで)、ラベル = 確定着順。取得時刻が発走前であることを行単位で検証 | 予測時点を 1 つに固定 (例: 10 分前)。学習・評価とも同じ時間帯のオッズを使う。**確定オッズで学習しない**。CL の ln_odds も同じ時点に差し替えて再採点する必要がある (再採点基盤が要る) |
| 入力期間 | 2026-07-12 以降の prospective 蓄積 (ability.db は使わない)。凍結抽出を封印 | 同上だが件数が桁で多く必要。ability.db の 2026H2 未使用制約とは別枠 (prospective benchmark) と明記 |
| 評価指標 | 一次: 検証期間の paired 勝馬 LogLoss 差 (候補 − 2 分前市場)。二次: top-k (1..3) 非悪化、人気帯別符号一致、各 stage 間の市場 LL 差 (30 vs 10 vs 2) の記述統計 | 一次: 固定時点での勝馬 LogLoss 差 (候補 CL − 現行 CL 同時点)、市場 top-k フロア、WIN5 母集団 paired、人気帯別 |
| 必要件数 | ロードマップ目安 800 レース (clean)。役割は **効果の有無の一次判定と、2,000R 段階の必要件数を決めるための分散推定** | D3 で測った開催日単位の分散と事前に決める最小実用差から算出 (§4)。ロードマップの 2,000 は仮置き |
| 出力 | HISTORICAL_SCREEN_PASS/FAIL/INCONCLUSIVE/INVALID と、日次分散の実測 | 採用ゲート (roadmap §8) の全項目 |
| 本番影響 | なし | 採用時のみ再学習と差し替え (別 SPEC・別登録) |

D3 が FAIL なら T17 本体は起票しない。PASS でも「特徴化の価値がある」以上の主張はしない。

## 3. 評価指標・情報時点・入力期間の整理 (v2 で凍結する内容)

### 3.1 母集団と除外 (行単位で機械判定)
1. 対象: JRA 平地 (芝/ダート)、8 頭以上、stage 30/10/2 の 3 段階が揃う。
2. 各 stage は「フラグ無し・`seconds_to_post` が [stage×60−60, stage×60+120] の窓内・`valid_odds_count = field_size`」の取得を 1 つ選ぶ (複数あれば最も発走に近いもの)。無い stage があればレース除外。
3. 発走後取得 (`seconds_to_post ≤ 0`) は無条件除外。
4. 結果: 全馬に `finish_position` があり、集合が {1..n}。30 分前以降の取消・除外があるレース (結果行数 ≠ 頭数) は **除外** (取消馬のオッズが市場確率を歪めるため)。同着は除外。
5. 除外理由を優先順 (取得欠落 → 窓外 → フラグ → オッズ不完全 → 結果不完全) で 1 つ付け、全フラグも記録。年月・場・頭数帯別の対象率を出す。

### 3.2 定義
- 市場確率 `p_s(i) = (1/odds_s(i)) / Σ_j (1/odds_s(j))`、s ∈ {30, 10, 2}。`odds` は取得値 (1.0 以下・非有限は品質エラー)。
- `drift(i) = log(odds_2(i) / odds_30(i))`、`dp(i) = p_2(i) − p_30(i)`、`rank_change(i) = pop_2(i) − pop_30(i)`。10 分前を使う派生 (`drift_late = log(odds_2/odds_10)`) は記述統計のみ。
- 候補 (レース内 conditional logit、係数は全レース共通、L2 なしの最尤 1 回、収束必須):
  (a) `log p_2` 係数 1 固定 (= 2 分前市場そのもの、対照)
  (b) `log p_2 + β1·drift`
  (c) `log p_2 + β1·drift + β2·dp + β3·rank_change`
  (d) `log p_2 + β4·(log p_model,30 − log p_30)` — p_model,30 は stage 30 の再解析の勝率。取得時刻差 ≤ 90 秒で対応付けられないレースは (d) の母集団から外し、件数を報告 (a〜c の母集団は変えない)
- 一次指標: 検証期間の `win_logloss_(b) − win_logloss_(a)` (レース等重み、開催日 block bootstrap 2,000 回・seed 17、95% CI)。
- 二次: (c)(d) の同差、top-k (k=1..3) の (a) 比、人気帯 (1-3 / 4-8 / 9+) 別の差の符号、`win_logloss_(市場30) − win_logloss_(市場2)` (時間帯で市場がどれだけ改善するかの記述統計)。

### 3.3 情報時点の保証
- 特徴は stage 取得値のみ。`observed_at < scheduled_post_at` を行単位で assert。
- ラベル (finish_position) は `race_results.result_fetched_at > scheduled_post_at` を assert。
- 学習/検証分割は開催日の時系列 60/40 (前 60% の開催日で係数推定、後 40% で評価) + 開催日 leave-one-out の paired。**検証期間の係数は固定**。
- 凍結抽出: `outputs/t17a/extract_<YYYYMMDD>.sqlite` (odds_snapshots の stage 30/10/2 行、race_results、predictions の stage 30 相当行、races) を read-only 抽出し sha256 を台帳に封入。以降の追加蓄積は本実験に混ぜない。

### 3.4 入力期間
- 開始: 2026-07-12 (stage 30/10/2 の取得開始)。終了: 凍結抽出日の前開催日。
- 欠測日 (9/13) はそのまま欠測。埋め戻し不可 (実時間データ)。
- 期間内のデータは T63 v2 (通知閾値)・T62b・T70 も読むが、いずれも読み取りで契約変更は無い。

## 4. 必要件数

### 4.1 D3 (800R)
- 目的は判定と分散推定の両方。800 clean レース (≈ 25〜28 開催日) で、開催日 block bootstrap の 95% CI 幅を報告する。
- 事前に「最小実用差」を置かない (探索診断)。ただし判定は §3.2 の一次指標の CI 下限 > 0 (PASS)、点推定 ≤ 0 (FAIL)、その他 INCONCLUSIVE。
- 到達見込み: 厳密 clean 570 → 800 まで 230 レース。10 月は 3 場開催 (週末 約 70 レース) なので **10 月中旬〜下旬** (10/18 または 10/25 の週末後)。

### 4.2 勝馬精度検証 (2,000R の妥当性)
必要開催日数は D3 で実測する日次平均 ΔLL の標準偏差 `SD_day` と、事前に決める最小実用差 `MDE` から
`N_days ≥ (1.96 × SD_day / (MDE / 2))^2` (95% CI 半幅を MDE の半分以下にする) で算出し、レース数に換算する。
- `MDE` の候補: 0.005 (T45 で観測した現行 CL と市場の LL 差 0.003〜0.05 の下限程度)。これを D3 の結果を見る前に決める。
- 2,000 レースは、SD_day ≈ 0.02・MDE = 0.005 なら N_days ≈ 61 日 ≈ 2,100 レース、と整合する仮置きに過ぎない。D3 の実測で置き換える。
- 現在のペース (10 月以降 週 70 レース) では 2,000 clean 到達は **2027 年 2〜3 月**。

## 5. SPEC-T17a v1 のレビュー所見 → v2 への修正

| # | 所見 | 修正 |
|---|---|---|
| 1 | 15/5 分前の段階も全レースで揃っている | 主診断は 30/10/2 のまま。15/5 は記述統計 (市場 LL の時間推移) にのみ使う |
| 2 | 除外規則が抽象的 | §3.1 のとおり行単位・優先順・全フラグ記録に具体化 |
| 3 | 発走後取得 (負の秒数) の存在 | 無条件除外を明記 |
| 4 | 30 分前以降の取消 (74 レース) | レース除外。取消を含む市場確率の再正規化はしない |
| 5 | 候補 (d) のモデル確率の時点が未定義 | stage 30 再解析の勝率に限定、取得時刻差 ≤ 90 秒、対応不能は (d) だけ母集団縮小・件数報告 |
| 6 | CL が ln_odds を含む (市場非独立) | (d) の解釈を「同時刻オッズを見たモデルの残差」に限定し、市場独立とは呼ばない |
| 7 | 必要件数の根拠が無い | §4 の二段階設計 (D3 で分散推定 → 本体の件数を算出) |
| 8 | L2 グリッドが暗黙 | L2 なし最尤 1 回 (候補数 3 + 対照 1)。探索なし |
| 9 | benchmark_type | `prospective` (蓄積データ)。ability.db の 2026H2 未使用制約とは別枠と明記 |
| 10 | 機械判定の INVALID 条件 | 凍結抽出 sha 不一致・発走後取得の混入・結果集合不一致・非収束・母集団 < 800 |
| 11 | 実装の段階分け | T81 と同じ Stage A (監査・隔離ハーネス・合成テスト・登録案) → 登録 → Stage B (1 回) |

## 6. まだ行わないこと
- 実データでの係数推定・LL 計算・相関 (smoke を含む)。
- 台帳登録 (登録案は Stage A の成果物として作る)。
- 本番・通知・T63/T62b/T70 契約の変更。

## 7. 次の手順
1. v2 SPEC のユーザー確認。
2. ゲート到達 (clean 800) 前でも、Stage A (ハーネス・合成テスト・凍結抽出の手順・登録案) は着手可能 → 指示があれば jra-coder に割り振る。
3. 到達後: 凍結抽出 → レビュー担当が登録 → Stage B 1 回 → 裁定 → (PASS なら) T17 本体の SPEC 起票。
