api/csv/*.csv
api/past_data.db
*.db
data_source/
__pycache__/
# 環境変数 (Neon接続文字列などの秘密情報)
.env

# ローカル専用の大容量データ (能力バックテスト用)
1980.csv
ability.db

# 分析の一時出力
sire_oos_pairs.csv
_scrape_res.json

# ローカル環境
venv/
.claude/
jra-web/
pedigree_cache.json

# Common prediction/odds logging database
data/jra_logging.db*

# T50 research-only JRA moisture/cushion archive and generated SQLite.
data/t50/

# T58 copyrighted scan extraction and rendered QA pages (local research only).
data/t58/

# T57 copyrighted book transcription and rendered QA pages (local research only).
data/t57/

# T59b public odds-board PoC fixtures (local research only).
data/t59/

# T54c official result HTML cache and local manifest/database artifacts.
data/t54c/
/outputs/t54c_*.json
/outputs/t54c_*.log
/outputs/t54c_*.ps1

# Generated locally after T33 adoption; production snapshot is never committed.
api/data_files/common/factor_snapshot.json

# T16 scratch place model (regenerated locally, never committed)
/web_place_model.json

# Direct upper-model<->Codex discussion transcripts (local only, not distributed).
.agents/
